{
  "name": "codebase-docs-ai",
  "version": "0.1.0",
  "private": true,
  "description": "Reusable AI documentation engine for analyzing source-code archives and generating structured technical documentation.",
  "type": "module",
  "packageManager": "pnpm@9.9.0",
  "engines": {
    "node": ">=20.10.0"
  },
  "scripts": {
    "build": "pnpm -r --sort build",
    "dev": "pnpm --parallel --filter @codebase-docs-ai/api --filter @codebase-docs-ai/web dev",
    "docker:build:api": "docker build --target api -t codebase-docs-ai-api:local .",
    "docker:build:web": "docker build --target web --build-arg VITE_WEB_API_BASE_URL=${VITE_WEB_API_BASE_URL:-http://localhost:3000} -t codebase-docs-ai-web:local .",
    "docker:compose:config": "docker compose config",
    "docker:smoke": "tsx scripts/docker-runtime-smoke.ts",
    "lint": "eslint .",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "smoke:e2e": "tsx scripts/e2e-smoke.ts",
    "test": "vitest run",
    "typecheck": "pnpm -r --sort typecheck",
    "web:completed-state": "vitest run apps/web/src/main.test.ts",
    "verify": "pnpm build && pnpm typecheck && pnpm test && pnpm lint && pnpm smoke:e2e"
  },
  "devDependencies": {
    "@eslint/js": "^9.16.0",
    "@types/node": "^22.10.1",
    "eslint": "^9.16.0",
    "jsdom": "^24.1.3",
    "prettier": "^3.4.2",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2",
    "typescript-eslint": "^8.17.0",
    "vitest": "^2.1.8"
  }
}
